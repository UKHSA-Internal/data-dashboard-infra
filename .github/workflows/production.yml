name: Production Workflow

on:
  push:
    branches:
      - "main"

env:
  AWS_REGION: "eu-west-2"

permissions:
  id-token: write
  contents: read

jobs:
  fast_forward_env_branches:
    name: Fast forward env branches
    runs-on: ubuntu-latest
    permissions:
      contents: write
    outputs:
      deploy_dev: ${{ steps.fast_forward_merge.outputs.deploy_dev }}
      deploy_test: ${{ steps.fast_forward_merge.outputs.deploy_test }}
      deploy_perf: ${{ steps.fast_forward_merge.outputs.deploy_perf }}
      deploy_pen: ${{ steps.fast_forward_merge.outputs.deploy_pen }}
      deploy_uat: ${{ steps.fast_forward_merge.outputs.deploy_uat }}
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - uses: ./.github/actions/setup-zsh

      - name: Fast forward env branches
        id: fast_forward_merge
        run: |
          - scripts/fast-forward-env-branches.sh
        shell: zsh {0}
        env:
          GH_TOKEN: ${{ github.token }}

  deploy_dev:
    needs: fast_forward_env_branches
    if: ${{needs.fast_forward_env_branches.outputs.deploy_dev}}
    uses: ./.github/workflows/well-known-environment.yml@env/dev/dev

  deploy_test:
    needs: fast_forward_env_branches
    if: ${{needs.fast_forward_env_branches.outputs.deploy_test}}
    uses: ./.github/workflows/well-known-environment.yml@env/test/test

  deploy_perf:
    needs: fast_forward_env_branches
    if: ${{needs.fast_forward_env_branches.outputs.deploy_perf}}
    uses: ./.github/workflows/well-known-environment.yml@env/test/perf

  deploy_pen:
    needs: fast_forward_env_branches
    if: ${{needs.fast_forward_env_branches.outputs.deploy_pen}}
    uses: ./.github/workflows/well-known-environment.yml@env/test/pen

  deploy_uat:
    needs: fast_forward_env_branches
    if: ${{needs.fast_forward_env_branches.outputs.deploy_uat}}
    uses: ./.github/workflows/well-known-environment.yml@env/uat/uat
